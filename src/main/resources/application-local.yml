# ===================================================================
# LOCAL OVERRIDE CONFIGURATION
# ===================================================================
spring:
  datasource:
    url: jdbc:postgresql://10.49.41.112:5432/core_service
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
  data:
    redis:
      host: 10.49.41.112
  security:
    user:
      name: admin
      password: "{noop}localpassword"
  cloud:
    config:
      enabled: false
  boot:
    admin:
      client:
        enabled: false
eureka:
  client:
    enabled: false
sa-token:
  is-log: true
logging:
  level:
    root: INFO
    vn.tr: INFO
    cn.dev33.satoken: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
    org.apache.kafka: ERROR
    org.springframework.kafka: ERROR
