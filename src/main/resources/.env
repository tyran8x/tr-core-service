# ===================================================================
# CẤU HÌNH MÔI TRƯỜNG LOCAL CHO TR-CORE-SERVICE
# File này không được commit lên Git. Hãy thêm .env vào .gitignore.
# ===================================================================

# Kích hoạt profile 'local' để sử dụng các cấu hình trong application-local.yml
# và các override từ file .env này.
SPRING_PROFILES_ACTIVE=local

# === Cấu hình Cơ sở dữ liệu (PostgreSQL) ===
# Các giá trị này sẽ ghi đè lên bất kỳ giá trị nào trong application.yml
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/dev_core_service
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=DnictPro@123

# === Cấu hình Redis ===
SPRING_DATA_REDIS_HOST=localhost
SPRING_DATA_REDIS_PORT=6379
SPRING_DATA_REDIS_PASSWORD=DnictPro@123

# === Cấu hình Bảo mật ===
# Rất quan trọng: Secret key để ký và xác thực JWT. PHẢI đủ dài và phức tạp.
# Key này PHẢI GIỐNG NHAU giữa Core-Service và các service khác (ví dụ: Catalog-Service).
JWT_SECRET_KEY=DayLaMotChuoiBiMatSieuDaiSieuPhucTapChoMoiTruongLocal123!@#$

# Tài khoản để truy cập các endpoint quản lý như /actuator/** và /sa-token/devops
# (Sử dụng bởi Spring Security, không phải Sa-Token)
SECURITY_USER_NAME=local-admin
SECURITY_USER_PASSWORD=local-strong-password-changeme

# === Cấu hình Logging ===
# Đường dẫn lưu file log cho môi trường local.
# Sử dụng đường dẫn tương đối để log được lưu trong thư mục dự án, dễ tìm kiếm.
LOGGING_FILE_PATH=./logs

# === Cấu hình khác (ví dụ) ===
# Có thể dùng để override các cấu hình khác cho local
# CAPTCHA_ENABLED=false
